@model List<Administrador>

<p class="h3">Administradores</p>

<div class="row mb-3">
    <div class="text-end">
        <a asp-action="cadastrar" class="btn btn-primary">
            <i class="fa-solid fa-plus"></i>Cadastrar
        </a>
    </div>
</div>

<table class="table table-striped table-bordered">
    <thead>

    <tr>
        <th>Nome</th>
        <th>Cpf</th>
        <th>Data de Nascimento</th>
        <th>Endereço</th>
        <th>E-mail</th>
        <th>Telefone</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var administrador in Model)
        {
            <tr>
                <td>@administrador.Nome</td>
                <td>@administrador.Cpf</td>
                <td>@administrador.DataNascimento.Date</td>
                <td>@administrador.Endereco</td>
                <td>@administrador.Email</td>
                <td>@administrador.Telefone</td>
                <td>
                   <a class="btn btn-primary" asp-action="editar" asp-route-id="@administrador.Id">
                    <i class="fa-solid fa-pencil"></i> Editar
                </a>
                <a class="btn btn-outline-danger" asp-action="apagar" asp-route-id="@administrador.Id">
                    <i class="fa-solid fa-trash"></i> Apagar
                </a>
                </td>
            </tr>
        }
    </tbody>
</table>